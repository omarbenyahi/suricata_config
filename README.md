# Suricata Configuration

This repository contains my Suricata IDS/IPS configuration files.

## Files included:
- `suricata.yaml` - Main configuration file
- `classification.config` - Rule classifications
- `reference.config` - Reference configuration
- `threshold.config` - Threshold configuration

## Usage
Copy these files to `/etc/suricata/` and restart Suricata service.
